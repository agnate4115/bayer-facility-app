name: Deploy to Azure

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy Backend to Azure Container Apps
        run: |
          az extension add --name containerapp --upgrade
          # Provide the secret environment variables via az containerapp up
          # We pass them as a list of key=secretref pairs, but for simplicity we inject them as env vars.
          az containerapp up \
            --name bayer-backend \
            --resource-group bayer-facility-rg \
            --location eastus \
            --environment bayer-env \
            --source ./backend \
            --ingress external \
            --target-port 8000 \
            --env-vars \
              "AZURE_OPENAI_BASE_URL=${{ secrets.AZURE_OPENAI_BASE_URL }}" \
              "AZURE_OPENAI_API_KEY=${{ secrets.AZURE_OPENAI_API_KEY }}" \
              "AZURE_OPENAI_DEPLOYMENT_NAME=${{ secrets.AZURE_OPENAI_DEPLOYMENT_NAME }}" \
              "AZURE_OPENAI_API_VERSION=${{ secrets.AZURE_OPENAI_API_VERSION }}" \
              "AZURE_STORAGE_CONNECTION_STRING=${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}" \
              "PGHOST=${{ secrets.PGHOST }}" \
              "PGUSER=${{ secrets.PGUSER }}" \
              "PGPORT=${{ secrets.PGPORT }}" \
              "PGDATABASE=${{ secrets.PGDATABASE }}" \
              "PGPASSWORD=${{ secrets.PGPASSWORD }}" \
              "ENABLE_AZURE_INTEGRATION=False"

  deploy-frontend:
    runs-on: ubuntu-latest
    needs: deploy-backend
    steps:
      - uses: actions/checkout@v3

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Provision Static Web App & Get Token
        id: swa
        run: |
          # Try to create the app. If it already exists, this command will safely error out or warn, which we ignore.
          az staticwebapp create -n bayer-frontend -g bayer-facility-rg -l eastus || true
          
          # Fetch the deployment token dynamically
          TOKEN=$(az staticwebapp secrets list -n bayer-frontend -g bayer-facility-rg --query "properties.apiKey" -o tsv)
          
          # Mask the token so it doesn't appear in logs
          echo "::add-mask::$TOKEN"
          echo "token=$TOKEN" >> $GITHUB_OUTPUT
          
          # Get the backend URL (the Container App FQDN)
          BACKEND_URL="https://$(az containerapp show -n bayer-backend -g bayer-facility-rg --query 'properties.configuration.ingress.fqdn' -o tsv)"
          echo "backend_url=$BACKEND_URL" >> $GITHUB_OUTPUT

      - name: Build And Deploy Frontend
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ steps.swa.outputs.token }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/app"
          api_location: ""
          output_location: "dist"
        env:
          VITE_API_URL: ${{ steps.swa.outputs.backend_url }}
